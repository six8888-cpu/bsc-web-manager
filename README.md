# BSC靓号生成器 - Web管理端

🚀 基于Web的BSC靓号地址生成器管理系统，支持远程管理多台服务器生成靓号。

---

## 📋 项目特点

- ✅ **Web界面管理** - 美观的Web界面，无需命令行操作
- ✅ **远程SSH控制** - 通过SSH连接管理B端服务器
- ✅ **实时输出显示** - WebSocket实时推送生成进度
- ✅ **自动环境配置** - 自动检测CPU、安装依赖
- ✅ **灵活参数设置** - 前缀、后缀、包含任意组合
- ✅ **多核心支持** - 拖拽式选择CPU核心数
- ✅ **结果自动下载** - 生成完成自动下载钱包文件

---

## 🏗️ 架构设计

```
┌─────────────────┐         SSH连接         ┌──────────────────┐
│  香港Web服务器   │ ────────────────────> │  B端服务器(中国)  │
│                 │                         │                  │
│  - Flask后端    │   1. 传输生成脚本       │  - Python环境     │
│  - 前端界面     │   2. 安装依赖          │  - 生成脚本       │
│  - WebSocket    │   3. 运行生成任务       │  - 输出结果       │
│  - 程序包存储   │   4. 实时获取输出       │                  │
└─────────────────┘   5. 下载结果文件      └──────────────────┘
        ↑
        │ HTTP + WebSocket
        ↓
   ┌─────────┐
   │  用户浏览器 │
   └─────────┘
```

---

## 📦 目录结构

```
bsc-web-manager/
├── backend/
│   ├── app.py                 # Flask后端主程序
│   └── requirements.txt       # 后端依赖
├── templates/
│   └── index.html            # 前端页面
├── static/
│   ├── css/
│   │   └── style.css         # 样式文件
│   └── js/
│       └── app.js            # 前端JS逻辑
├── bsc_generator/
│   ├── ultra_generator_v2.py # 生成脚本（会传输到B端）
│   └── requirements.txt      # 生成器依赖
├── deploy/
│   ├── install_web.sh        # Web端部署脚本
│   ├── install_client.sh     # B端安装脚本
│   └── start.sh              # 快速启动脚本
├── output/                   # 生成的钱包文件存储目录
└── README.md                 # 本文件
```

---

## 🚀 部署指南

### 方案一：一键部署（推荐）

**香港Web服务器：**

```bash
# 下载项目
git clone <your-repo-url>
cd bsc-web-manager

# 运行部署脚本
chmod +x deploy/install_web.sh
sudo ./deploy/install_web.sh

# 启动服务
sudo systemctl start bsc-web
sudo systemctl enable bsc-web
```

**访问地址：** `http://your-server-ip:5000`

### 方案二：手动部署

```bash
# 1. 安装依赖
sudo apt update
sudo apt install -y python3 python3-pip python3-venv

# 2. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 3. 安装Python包
cd backend
pip install -r requirements.txt

# 4. 启动服务
python app.py
```

### 方案三：快速启动（开发测试）

```bash
chmod +x deploy/start.sh
./deploy/start.sh
```

---

## 📖 使用说明

### 1. 配置服务器连接

在Web界面左侧填写：
- **服务器IP**: B端服务器的公网IP
- **端口**: SSH端口（默认22）
- **用户名**: SSH用户名（默认root）
- **密码**: SSH密码

点击 **"🔍 测试连接"** 验证连接。

### 2. 设置生成参数

**前缀 (Prefix):**
```
例如: 1780
生成地址: 0x1780abc...
```

**后缀 (Suffix):**
```
例如: 3CffbD
生成地址: 0x...3CffbD
```

**包含 (Contains):**
```
例如: 888
生成地址: 0x...888...
```

**其他选项:**
- ✅ 区分大小写
- 🔢 生成数量（默认1个）
- ⚙️ CPU核心数（拖拽滑块）

### 3. 开始生成

点击 **"🚀 开始生成"** 按钮，系统会：

1. SSH连接到B端服务器
2. 上传生成脚本和依赖文件
3. 自动安装Python依赖（首次）
4. 运行生成任务
5. 实时显示输出
6. 自动下载结果

### 4. 下载结果

生成完成后，点击 **"💾 下载钱包文件"** 保存结果。

---

## ⚙️ 配置说明

### Web服务器配置

**修改端口:** 编辑 `backend/app.py` 最后一行
```python
socketio.run(app, host='0.0.0.0', port=5000, debug=False)
```

**修改存储目录:** 编辑 `backend/app.py`
```python
output_dir = os.path.join(os.path.dirname(__file__), '../output')
```

### B端服务器要求

**最低要求:**
- 系统: Linux (Debian/Ubuntu/CentOS)
- Python: 3.7+
- 内存: 1GB+
- CPU: 任意（越多越快）

**推荐配置:**
- 8核16GB: 适合6位靓号
- 32核64GB: 适合8位靓号
- 64核以上: 适合10位靓号

---

## 🔐 安全建议

1. **使用密钥认证** - 替代密码SSH登录更安全
2. **限制访问IP** - 在防火墙限制Web访问来源
3. **使用HTTPS** - 配置Nginx反向代理+SSL证书
4. **定期更新** - 及时更新系统和依赖包
5. **私钥保护** - 下载的钱包文件务必加密存储

---

## 🐛 故障排查

### 问题1: 连接B端失败

**原因:**
- IP地址错误
- SSH端口未开放
- 防火墙拦截
- 用户名密码错误

**解决:**
```bash
# 在B端服务器检查SSH服务
sudo systemctl status sshd

# 开放SSH端口
sudo ufw allow 22/tcp
```

### 问题2: 依赖安装失败

**原因:**
- 网络问题
- pip源速度慢

**解决:**
```bash
# 使用国内pip源
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

### 问题3: WebSocket断开

**原因:**
- 网络不稳定
- Nginx代理配置问题

**解决:**
```nginx
# Nginx配置WebSocket支持
location /socket.io {
    proxy_pass http://127.0.0.1:5000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

---

## 🔧 高级配置

### 使用Nginx反向代理

```bash
# 安装Nginx
sudo apt install nginx

# 配置
sudo nano /etc/nginx/sites-available/bsc-web
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /socket.io {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/bsc-web /etc/nginx/sites-enabled/
sudo systemctl restart nginx
```

### 配置SSL证书

```bash
# 使用Let's Encrypt
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 📊 性能参考

| 靓号长度 | 理论概率 | 推荐配置 | 预估时间 | 成本 |
|---------|---------|---------|---------|------|
| 4位 | 1/6.5万 | 本地电脑 | 几秒 | 免费 |
| 6位 | 1/1677万 | 8核 | 1-5分钟 | ¥50/月 |
| 8位 | 1/4.3万亿 | 32核 | 1-3小时 | ¥200/月 |
| 10位 | 1/1.1千万亿 | 64核+ | 3-15天 | ¥500+/月 |

---

## 📄 开源协议

MIT License - 自由使用，无需授权

---

## 🤝 贡献

欢迎提交Issues和Pull Requests！

---

## 📧 联系方式

- 项目地址: <your-repo-url>
- 原项目: https://github.com/six8888-cpu/bsclianghao

---

**⭐ 如果这个项目对你有帮助，请给个Star支持一下！**

